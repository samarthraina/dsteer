# Pinned versions matching the environment that worked on vast.ai A800 80GB.
# CUDA 12.6 driver, Python 3.12.

# vLLM and its compatible transformers ecosystem.
# vllm 0.9.2 is the highest version built against CUDA 12 (0.10+ requires CUDA 13).
vllm==0.9.2
transformers==4.49.0
tokenizers==0.21.4
huggingface-hub==0.36.2
accelerate

# Core ML
torch==2.7.0
torchvision==0.22.0
torchaudio==2.7.0

# Data + analysis
datasets
numpy
pandas
matplotlib
scipy

# Eval-specific
absl-py
langdetect
nltk
immutabledict

# Utility
tqdm
pyyaml
openai
